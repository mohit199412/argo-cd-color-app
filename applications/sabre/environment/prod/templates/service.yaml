apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.hostspacespec.projectName }}"
  labels:
    run: "{{ .Values.hostspacespec.projectName }}"
spec:
  ports:
  - port: 8080
    protocol: TCP
  selector:
    app: "{{ .Values.hostspacespec.projectName }}"
  type: LoadBalancer