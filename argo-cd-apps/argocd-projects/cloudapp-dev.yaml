apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudapp-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/mohit199412/argo-cd-color-app.git
    targetRevision: argo-cd-test
    path: applications/cloudapp/environment/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: cloudapp-appspace
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true